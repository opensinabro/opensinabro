# the seed와 우리 렌더의 알려진 차이. `parity compare`가 이 목록에 걸리는 차이는
# 통과시키고 새 차이만 보고한다. 한 줄에 패턴 하나이며, 정규화된 조각 문자열에
# 부분 일치하면 알려진 차이로 본다.
#
# 새 항목을 넣을 때는 반드시 근거(왜 다른지, 의도된 것인지)를 주석으로 함께 남긴다.
# 배경은 tools/parity/README.md 참고.


# 문단 편집 링크. the seed는 헤딩마다 [편집] 링크를 넣는다. 위키 서버 UI이고
# 마크업 렌더링 산출물이 아니므로 백엔드가 낼 대상이 아니다.
/edit/
wiki-edit-section
&#91;편집&#93;
[편집]

# 문단명 앵커(<span id='개요'>). the seed는 헤딩 제목을 span으로 감싸 `[[#개요]]`용
# 앵커를 만든다. 우리는 `s-N` 앵커만 방출한다 — 문단명 앵커는 후속 과제다.
# (docs/spec/implementation-status.md 참고)
<span id=

# 목차 접힘 표시. the seed는 빈 <summary>에 스킨 CSS로 "목차"를 넣고,
# 우리는 동봉 CSS(summary::after)로 같은 효과를 낸다. 마크업은 양쪽 다 빈 summary다.

# `[pagecount]`(위키의 문서 수). 위키 DB에 달린 값이라 마크업 렌더링으로 낼 수 없다 —
# the seed는 `632522` 같은 실제 수를 내고 우리는 값을 몰라 원문을 그대로 남긴다.
# 이름공간별 변형(`[pagecount(문서)]` 등)도 같다.
[pagecount

# `[date]`/`[datetime]`(접속 시각). the seed는 렌더 시각을 `<time datetime='…'>`으로 낸다.
# 코퍼스는 the seed가 렌더한 그 순간을 알 수 없어 대조 대상이 아니다.
[date]
[datetime]

# `[age(날짜)]`·`[dday(날짜)]`(나이·D-day). the seed는 렌더 시점 기준으로 계산해
# (`설립된 지 11년`, `+3218일`) 내지만 우리는 그 순간을 알 수 없어 원문을 남긴다.
# [date]와 같은 부류다 — 렌더 시각에 달린 값이라 코퍼스 대조 대상이 아니다.
[age(
[dday(
[dday]

# 하이픈 10개짜리 줄(`----------`). 4~9개는 `<hr>`이고 10개는 아닌데, the seed는 이걸
# `<del></del>----`로 낸다 — 취소선 마커가 `--`인데도 del이 6글자를 먹고 4글자가 남는
# 셈이라 규칙을 확정하지 못했다. 미결(추측으로 맞추지 않는다).
"----------"

